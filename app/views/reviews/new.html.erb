<h1> Creating a review for <%= @restaurant.name %> </h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, collection: Review::RATING %>
  <%= f.input :content %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to #{@restaurant.name}", restaurant_path(@restaurant) %>
